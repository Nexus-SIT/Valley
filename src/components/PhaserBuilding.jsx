// PhaserBuilding.jsx — RETIRED
// The Phaser canvas overlay approach has been replaced.
// Building 1 is now rendered directly inside the main Phaser game in GameCanvas.jsx.
const PhaserBuilding = () => null;
export default PhaserBuilding;
